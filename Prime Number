a = input('Enter the value: ');
isPrime = 0;
for i=2:a-1
    if(mod(a,i) == 0)
       isPrime = 1;
    end
end

if(isPrime == 0)
    disp('Entered number is prime')
else
    disp('Entered number is not prime')
end
